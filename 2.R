add_numbers <- function(x, y) {
  return(x + y)
}



x <- as.numeric(readline("Enter first no. "))
y <- as.numeric(readline("Enter Second no. "))
print(paste("Result = ",add_numbers(x,y)))

mydf <- data.frame((
  Name = c("John, Ra")
))